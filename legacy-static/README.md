# Legacy Static Site

This folder contains the old hand-written static site files that used to live at the repository root.

They are kept for reference only. The live site source is the Astro app in `src/`, and Cloudflare Pages builds that app into `dist/`.
